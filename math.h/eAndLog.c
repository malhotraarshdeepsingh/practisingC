#include <stdio.h>
#include <math.h>

int main() {
    double x = 1.0;
    printf("exp(1) = %f\n", exp(x));  // e^1 = 2.718...

    double a = 10.0;
    printf("log(10) = %f\n", log(a));     // Natural log
    printf("log10(10) = %f\n", log10(a)); // Base-10 log

    return 0;
}