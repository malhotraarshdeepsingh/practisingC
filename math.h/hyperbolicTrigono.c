// Compute the hyperbolic sine, cosine, and tangent of x.

#include <stdio.h>
#include <math.h>

int main() {
    double x = 1.0;
    printf("sinh(1) = %f\n", sinh(x));
    printf("cosh(1) = %f\n", cosh(x));
    printf("tanh(1) = %f\n", tanh(x));
    return 0;
}