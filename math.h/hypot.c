// hypot() Computes the length of the hypotenuse of a right-angle triangle given the other two sides x and y.

#include <stdio.h>
#include <math.h>

int main() {
    printf("hypot(3, 4) = %f\n", hypot(3.0, 4.0));
    return 0;
}